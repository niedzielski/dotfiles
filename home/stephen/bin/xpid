#!/usr/bin/env bash

xpid() {
  xprop -f _NET_WM_PID 0c ' = $0\n' _NET_WM_PID|sed -r 's%.* = %%'
}

# ------------------------------------------------------------------------------
if ! return 2>/dev/null; then
  xpid "$@"
fi