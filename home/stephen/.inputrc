# ------------------------------------------------------------------------------
# .inputrc
# Copyright 2011 Stephen Niedzielski. Licensed under GPLv3+.

# ------------------------------------------------------------------------------
# Misc

# Silent mode.
set bell-style none

# I don't quite understand these bits, but it's seems the optimal English
# configuration is below:
set input-meta on
set output-meta on
set convert-meta off
set enable-keypad on

# ------------------------------------------------------------------------------
# Completion

# Be as understanding as possible, case insenstive.
set completion-ignore-case on

# Work like mark-directory by appending a slash to symlinked dirs too.
set mark-symlinked-directories on

# Don't wait for another tab, just complete it. Also: just give me the beans!
set show-all-if-ambiguous on
set show-all-if-unmodified on

# If attempting completion midword and word is already complete, just skip the
# completion and jump to the end of the word. Unfortunately, this doesn't pull
# in the righthand side when a word is incomplete.
set skip-completed-text on

# TODO: need quote mechanism.
# TODO: read default shortcuts.
# TODO: man bash.

# Unlimited history.
set history-size 0

# Append a character denoting filetype to possible completion lists.
set visible-stats on

# c-left, c-right.
"\e[1;5C": forward-word
"\e[1;5D": backward-word

# c-del, c-] (c-bs == bs and c-\ is some kind of signal).
"\e[3;5~": kill-word'
"\C-]": backward-kill-word

# TODO: why isn't this working?
Control-s: forward-search-history
#undo is ctrl_

# TODO: investigate highlighting / marking for c-s-left, c-s-right, ...
#"\e[1;6C": ...
#"\e[1;6D": ...

# TODO: quote param.
#"\C-xq": "\eb\"\ef\""

# TODO: turn on home adjustment by readline since not always using bash?
# consider binding fxchpp|x g to ctrl-f, for ex

# TODO: info rluserman

# I prefer vi-style editing:
set editing-mode vi

Tab: complete
