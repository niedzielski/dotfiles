# configures environment for android dev

export ANDROID="$HOME/opt/android/sdk"
export ANDROID_SDK="$ANDROID"

set_path_append_dirs "$ANDROID/tools" \
                     "$ANDROID/platform-tools" \
                     "$(glob_last_dir "$ANDROID/build-tools/"*)"